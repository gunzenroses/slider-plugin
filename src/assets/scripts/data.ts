import { Settings } from "./types/types"

let sliderData: Settings = {
    min: 0,
    max: 400,
    range: true,
    currentFirst: 33,
    currentSecond: 66,
    step: 1,
    orientation: "horizontal",
    //label: false,
    //scale: false,
}

export { sliderData }